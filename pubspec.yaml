name: nabiz
description: NABIZ — doğru anda dokun, combo yap. Hyper-casual zamanlama oyunu.
publish_to: "none"
version: 1.0.0+6

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
