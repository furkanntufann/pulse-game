# Codemagic — Pulse iOS build
# https://codemagic.io — 14 gün deneme
# İmzalama: Codemagic UI → iOS code signing (Apple Developer hesabı gerekir)

workflows:
  pulse-ios:
    name: Pulse iOS
    max_build_duration: 60
    instance_type: mac_mini_m2

    environment:
      flutter: stable
      xcode: latest
      ios_signing:
        distribution_type: development
        bundle_identifier: com.tufan.pulsegame

    scripts:
      - name: Bağımlılıklar ve iOS şeması
        script: |
          flutter config --no-enable-swift-package-manager
          if [ ! -f "ios/Podfile" ]; then
            echo "Podfile eksik, iOS klasoru yeniden olusturuluyor..."
            flutter create . --org com.tufan --project-name nabiz --platforms=ios
          fi
          if ! grep -q "platform :ios" "ios/Podfile"; then
            echo "platform :ios, '13.0' ekleniyor..."
            sed -i.bak "1s;^;platform :ios, '13.0'\n\n;" "ios/Podfile"
          fi
          flutter pub get
          if [ ! -f "ios/Runner.xcodeproj/xcshareddata/xcschemes/Runner.xcscheme" ]; then
            echo "Runner.xcscheme repoda yok — ios klasoru tamamlaniyor..."
            flutter create . --org com.tufan --project-name nabiz --platforms=ios
          fi
          test -f "ios/Runner.xcodeproj/xcshareddata/xcschemes/Runner.xcscheme" || exit 1
      - name: iOS IPA derle
        script: flutter build ipa --release

    artifacts:
      - build/ios/ipa/*.ipa

  pulse-android:
    name: Pulse Android APK
    max_build_duration: 30
    instance_type: mac_mini_m2

    environment:
      flutter: stable

    scripts:
      - name: Bağımlılıklar
        script: flutter pub get
      - name: Android APK
        script: flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
